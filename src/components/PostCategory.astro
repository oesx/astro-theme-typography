---
interface Props {
  category: string
}

const { category } = Astro.props
---

<a
  href={`/categories/${category}`}
  class="rounded bg-gray-100 px-2 py-1 text-sm not-underline-hover dark:bg-gray-800"
>
  {category}
</a>
